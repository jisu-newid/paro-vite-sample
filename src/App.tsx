function App() {

    return (
        <div className={'relative mx-auto flex h-[100dvh] flex-col items-center justify-center gap-3'}>
            <h1>Vite + React + Tailwindcss</h1>
        </div>
    );
}

export default App;
